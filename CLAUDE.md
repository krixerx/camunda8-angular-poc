# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository is a newly created, empty project (no commits yet). Based on its name, it is intended to become a proof of concept integrating **Camunda 8** (process orchestration) with an **Angular** frontend. The only file present is an Eclipse `.project` descriptor.

There is no build system, source code, or test setup yet. Once the project is scaffolded (e.g., with Angular CLI and Camunda 8 client libraries), regenerate this file with `/init` so it reflects the actual commands and architecture.
